# SoriMemo 작업 로그 - 2026-06-20

## 작업 범위

- 서비스 명칭을 외부 화면 기준으로 `SoriMemo`, 서비스 설명 기준으로 `안심소리 기억케어`로 정리했습니다.
- 개발 서버 운영 기준을 `sorimemo-backend`, `/var/www/sorimemo`, `sorimemo_dev` 흐름으로 맞췄습니다.
- 로그인, 회원가입, 동의 절차, 서비스 시작, 부모님/본인 검증 흐름의 화면 문구와 글자 크기를 시니어 사용자 기준으로 보완했습니다.
- 검증 대상 정보 화면에서 관계 선택과 표시명 입력이 중복되지 않도록 정리했습니다.
- 관리자 페이지에 사용자, 음성 데이터, 분석 결과, 분석 상세, AI 모델, 관리자 로그, 환경 설정, 학습 데이터 관리 화면을 보완했습니다.
- 관리자 분석 결과는 서비스 화면의 참고값 표시와 맞추고, 새로고침 상태 메시지를 표시하도록 수정했습니다.
- 선택 동의 기반의 학습용 장기 보관 정책을 추가하고, 동의한 원본 음성은 기본 삭제 정책과 분리해 `training_retained` 상태로 남기도록 백엔드 처리 흐름을 보완했습니다.

## DB 및 운영 작업

- `care_subjects` 추가 마이그레이션을 준비했습니다.
- `consent_sessions`에 학습 활용 동의 여부와 보관 기간 컬럼을 추가하는 마이그레이션을 준비했습니다.
- 관리자 계정 재설정 스크립트와 개발 DB 권한 설정 흐름을 점검했습니다.
- Nginx 개발 설정과 systemd 서비스 파일을 SoriMemo 명칭 기준으로 추가했습니다.

## 검증

- 백엔드 Python 컴파일 검증을 수행했습니다.
- 프론트엔드 Vite 빌드를 수행했고 정상 빌드되는 것을 확인했습니다.
- 실제 개발 서버에서 `/healthz`, `/api/consent/policy`, 로그인/관리자 화면 흐름을 점검했습니다.

## 백업

- Git 동기화 전 현재 작업 파일 백업을 생성했습니다.
- 백업 파일: `/tmp/sorimemo_git_worktree_backup_20260620_235459.tar.gz`
- 백업은 Git 추적 대상 및 신규 작업 파일 중심으로 만들었고, `.git`, `tools`, `node_modules`, 빌드 산출물, DB 비밀번호, 업로드/로그/운영 백업 파일은 제외했습니다.

## GitHub 동기화 계획

- 기존 Velora 원격 저장소는 유지합니다.
- 새 원격 저장소 `smartcoresolution/sorimemo.git`를 별도 remote로 추가합니다.
- 민감정보와 빌드 산출물을 제외한 코드, 문서, 운영 설정, 마이그레이션만 커밋 후 새 저장소의 `main` 브랜치로 push합니다.
